# If your API is served from same origin via Vite proxy, you can leave this commented
# VITE_API_BASE_URL=/api

# Dev proxy target used by Vite (only for `npm run dev`)
# Set to your backend origin, e.g. http://localhost:3000
# VITE_PROXY_TARGET=http://localhost:3000
